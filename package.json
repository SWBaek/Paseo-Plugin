{
  "name": "paseo-plugins-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "plugins/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
